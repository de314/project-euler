const { expect } = require('chai');
const runner = require('../src/problems/<%= paddedId %>.js');

describe('Problem #<%= problem.id %>', function() {
  it('should satisfy example', function() {
    const input = 10;
    const expected = 23;
    const actual = runner.test(input);
    expect(actual).to.eq(expected);
  });
});
